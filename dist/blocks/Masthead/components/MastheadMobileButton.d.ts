import { FC } from 'react';
interface MastheadMobileButtonProps {
    isOpen?: boolean;
    setIsOpen: (isOpen: boolean) => void;
}
declare const MastheadMobileButton: FC<MastheadMobileButtonProps>;
export default MastheadMobileButton;
