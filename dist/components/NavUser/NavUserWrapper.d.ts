import React, { FC } from 'react';
export interface NavUserWrapper {
    className?: string;
    children?: React.ReactNode;
}
declare const NavUserWrapper: FC<NavUserWrapper>;
export default NavUserWrapper;
